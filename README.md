# block-explorers
##### Install
```
pipenv install
```
Run `python crawler_alpha.py`

And then run `python server.py`


### HTTP API
##### Get info from API
```
curl http://localhost:3001/blocks
```

